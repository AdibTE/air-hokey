# Optional local defaults. Copy to .env if you want a local file:
#   copy .env.example .env
#
# On Render: set these in the service Environment tab (not via a committed .env).

PORT=4000
CORS_ORIGIN=*
